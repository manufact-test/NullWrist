name: Apply Stage 1 stabilization patch

on:
  push:
    branches:
      - agent/0.8.10-reliable-runtime
    paths:
      - .github/workflows/stabilize-stage1.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/0.8.10-reliable-runtime

      - name: Patch runtime lifecycle and sleep behavior
        run: |
          python3 <<'PY'
          from pathlib import Path

          def replace_once(path, old, new):
              file = Path(path)
              text = file.read_text()
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f"Expected one match in {path}, found {count}: {old[:80]!r}")
              file.write_text(text.replace(old, new, 1))

          service_path = "app/src/main/java/com/manufacttest/pebblereardisplay/runtime/PebbleRuntimeService.java"

          replace_once(
              service_path,
              "import android.app.Notification;\n",
              "import android.app.AlarmManager;\nimport android.app.Notification;\n"
          )
          replace_once(
              service_path,
              "import android.os.Looper;\n",
              "import android.os.Looper;\nimport android.os.SystemClock;\n"
          )
          replace_once(
              service_path,
              '''    private static final String ACTION_APPLY_RUNTIME_MODE =
              "com.manufacttest.pebblereardisplay.action.APPLY_RUNTIME_MODE";
''',
              '''    private static final String ACTION_APPLY_RUNTIME_MODE =
              "com.manufacttest.pebblereardisplay.action.APPLY_RUNTIME_MODE";
    private static final String ACTION_RECOVER_TASK =
              "com.manufacttest.pebblereardisplay.action.RECOVER_AFTER_TASK_REMOVAL";
'''
          )
          replace_once(
              service_path,
              '''    private static final String NOTIFICATION_CHANNEL_ID = "pebblehertz_runtime";
    private static final int NOTIFICATION_ID = 168;
''',
              '''    private static final String NOTIFICATION_CHANNEL_ID = "pebblehertz_runtime";
    private static final int NOTIFICATION_ID = 168;
    private static final int RECOVERY_REQUEST_CODE = 810;
    private static final long RECOVERY_DELAY_MILLIS = 1_200L;
'''
          )
          replace_once(
              service_path,
              '''    private volatile boolean foreground;
    private volatile String notificationText = "Starting PebbleOS…";
''',
              '''    private volatile boolean foreground;
    private volatile String notificationText = "Rear watchface active";
    private volatile boolean selectionQueuedForWake;
    private volatile boolean schedulePaused;
'''
          )
          replace_once(
              service_path,
              '''        return null;
    }

    public static void addListener(Listener listener) {
''',
              '''        return null;
    }

    public static boolean isSelectionQueuedForWake() {
        PebbleRuntimeService current = instance;
        return current != null && current.selectionQueuedForWake;
    }

    public static void addListener(Listener listener) {
'''
          )
          replace_once(
              service_path,
              '''    @Override
    public int onStartCommand(Intent intent, int flags, int startId) {
        String action = intent == null ? ACTION_START : intent.getAction();
        if (ACTION_STOP.equals(action)) {
            generation.incrementAndGet();
            stopRuntime();
            demoteFromForeground();
            stopSelf();
            return START_NOT_STICKY;
        }
        if (ACTION_APPLY_RUNTIME_MODE.equals(action)) {
            applyRuntimeModeInternal(notificationText);
            notifyListeners();
            return START_STICKY;
        }
        if (ACTION_SELECT.equals(action)) {
            scheduleSelection();
        } else if (ACTION_REFRESH_POWER.equals(action)) {
            applyPowerPolicy();
        } else {
            scheduleRuntime(ACTION_RESTART.equals(action));
        }
        return START_STICKY;
    }
''',
              '''    @Override
    public int onStartCommand(Intent intent, int flags, int startId) {
        String action = intent == null ? ACTION_START : intent.getAction();
        if (ACTION_STOP.equals(action)) {
            cancelTaskRemovalRecovery();
            generation.incrementAndGet();
            stopRuntime();
            demoteFromForeground();
            stopSelf();
            return START_NOT_STICKY;
        }
        if (ACTION_APPLY_RUNTIME_MODE.equals(action)) {
            applyRuntimeModeInternal(notificationText);
            notifyListeners();
            return START_STICKY;
        }
        if (ACTION_RECOVER_TASK.equals(action)) {
            applyRuntimeModeInternal("Rear watchface active");
            scheduleRuntime(false);
            return START_STICKY;
        }
        if (ACTION_SELECT.equals(action)) {
            scheduleSelection();
        } else if (ACTION_REFRESH_POWER.equals(action)) {
            applyPowerPolicy();
        } else {
            scheduleRuntime(ACTION_RESTART.equals(action));
        }
        return START_STICKY;
    }
'''
          )
          replace_once(
              service_path,
              '''    @Override
    public IBinder onBind(Intent intent) {
        return null;
    }

    @Override
    public void onDestroy() {
''',
              '''    @Override
    public IBinder onBind(Intent intent) {
        return null;
    }

    @Override
    public void onTaskRemoved(Intent rootIntent) {
        scheduleTaskRemovalRecovery();
        super.onTaskRemoved(rootIntent);
    }

    @Override
    public void onDestroy() {
'''
          )
          replace_once(
              service_path,
              '''    private void scheduleSelection() {
        int requestedGeneration = generation.get();
        executor.execute(() -> applyLatestSelection(requestedGeneration));
    }
''',
              '''    private void scheduleSelection() {
        int requestedGeneration = generation.get();
        executor.execute(() -> applyLatestSelection(requestedGeneration));
    }
'''
          )
          replace_once(
              service_path,
              '''        if (requestedGeneration != generation.get()) {
            return;
        }

        PebbleQemuProcess current = runtime;
''',
              '''        if (requestedGeneration != generation.get()) {
            return;
        }

        RuntimePowerPolicy.Snapshot snapshot = RuntimePowerPolicy.evaluate(
                this,
                new AppPreferences(this)
        );
        powerMode = snapshot.mode;
        if (snapshot.batteryPercent >= 0) {
            phoneBatteryPercentage = snapshot.batteryPercent;
        }
        phoneChargerConnected = snapshot.charging;
        if (RuntimeSelectionPolicy.shouldQueue(snapshot.mode, snapshot.charging)) {
            selectionQueuedForWake = true;
            thumbnailGeneration.incrementAndGet();
            notifyListeners();
            return;
        }

        PebbleQemuProcess current = runtime;
'''
          )
          replace_once(
              service_path,
              '''        activeStorageId = selected.metadata.getStorageId();
        starting = false;
        status = null;
''',
              '''        activeStorageId = selected.metadata.getStorageId();
        selectionQueuedForWake = false;
        schedulePaused = false;
        starting = false;
        status = null;
'''
          )
          replace_once(
              service_path,
              '''    private void applyPowerPolicy() {
        RuntimePowerPolicy.Snapshot snapshot = RuntimePowerPolicy.evaluate(
                this,
                new AppPreferences(this)
        );
        RuntimePowerPolicy.Mode previousMode = powerMode;
        powerMode = snapshot.mode;
        policyHandler.removeCallbacks(lowBatteryPause);
        policyHandler.removeCallbacks(lowBatteryPulse);

        PebbleQemuProcess current = runtime;
        if (runtimeBusy || current == null || !current.isRunning()) {
            notifyListeners();
            return;
        }

        try {
            if (snapshot.mode == RuntimePowerPolicy.Mode.RUNNING) {
                current.resume();
                if (previousMode != snapshot.mode) {
                    updateNotification(activeStorageId == null
                            ? "Pebble Time is running"
                            : "Running selected watchface");
                }
            } else if (snapshot.mode == RuntimePowerPolicy.Mode.SCHEDULED_FREEZE) {
                current.pause();
                AppPreferences preferences = new AppPreferences(this);
                updateNotification(
                        "Sleeping until "
                                + AppPreferences.formatMinutes(
                                preferences.getSleepEndMinutes()
                        )
                );
            } else {
                current.resume();
                policyHandler.postDelayed(lowBatteryPause, LOW_BATTERY_AWAKE_MILLIS);
                scheduleNextLowBatteryPulse();
                updateNotification("Battery saver · updates each minute");
            }
            status = null;
            failure = null;
            notifyListeners();
        } catch (IOException error) {
            reportFailure(error);
        }
    }
''',
              '''    private void applyPowerPolicy() {
        RuntimePowerPolicy.Snapshot snapshot = RuntimePowerPolicy.evaluate(
                this,
                new AppPreferences(this)
        );
        RuntimePowerPolicy.Mode previousMode = powerMode;
        powerMode = snapshot.mode;
        if (snapshot.batteryPercent >= 0) {
            phoneBatteryPercentage = snapshot.batteryPercent;
        }
        phoneChargerConnected = snapshot.charging;
        policyHandler.removeCallbacks(lowBatteryPause);
        policyHandler.removeCallbacks(lowBatteryPulse);

        PebbleQemuProcess current = runtime;
        if (runtimeBusy || current == null || !current.isRunning()) {
            notifyListeners();
            return;
        }

        boolean applyQueuedSelection = RuntimeSelectionPolicy.shouldApplyQueuedSelection(
                previousMode,
                snapshot.mode,
                selectionQueuedForWake
        );
        try {
            if (snapshot.mode == RuntimePowerPolicy.Mode.SCHEDULED_FREEZE) {
                if (!schedulePaused) {
                    thumbnailGeneration.incrementAndGet();
                    current.pause();
                    schedulePaused = true;
                }
            } else {
                if (schedulePaused) {
                    current.resume();
                    schedulePaused = false;
                }
                if (snapshot.mode == RuntimePowerPolicy.Mode.RUNNING) {
                    if (previousMode != snapshot.mode) {
                        updateNotification("Rear watchface active");
                    }
                } else {
                    current.resume();
                    policyHandler.postDelayed(lowBatteryPause, LOW_BATTERY_AWAKE_MILLIS);
                    scheduleNextLowBatteryPulse();
                    updateNotification("Rear watchface active");
                }
            }
            status = null;
            failure = null;
            notifyListeners();
        } catch (IOException error) {
            reportFailure(error);
            return;
        }

        if (applyQueuedSelection) {
            scheduleSelection();
        }
    }
'''
          )

          file = Path(service_path)
          text = file.read_text()
          old = '''                runtime = null;
                activeStorageId = null;
'''
          if text.count(old) != 1:
              raise SystemExit(f"Unexpected discard runtime block count: {text.count(old)}")
          text = text.replace(
              old,
              '''                runtime = null;
                activeStorageId = null;
                selectionQueuedForWake = false;
                schedulePaused = false;
''',
              1
          )
          old = '''        runtime = null;
        activeStorageId = null;
        starting = false;
'''
          if text.count(old) != 1:
              raise SystemExit(f"Unexpected stop runtime block count: {text.count(old)}")
          text = text.replace(
              old,
              '''        runtime = null;
        activeStorageId = null;
        selectionQueuedForWake = false;
        schedulePaused = false;
        starting = false;
''',
              1
          )
          file.write_text(text)

          replace_once(
              service_path,
              '''    private void updateNotification(String text) {
        notificationText = text == null || text.isBlank()
                ? "Pebble Time is running"
                : text;
        if (!new AppPreferences(this).isReliableRuntime()) {
            return;
        }
        if (!foreground) {
            promoteToForeground(notificationText);
            return;
        }
        NotificationManager manager = getSystemService(NotificationManager.class);
        if (manager != null) {
            manager.notify(NOTIFICATION_ID, buildNotification(notificationText));
        }
    }

    private void applyRuntimeModeInternal(String text) {
        if (new AppPreferences(this).isReliableRuntime()) {
            promoteToForeground(text);
        } else {
            demoteFromForeground();
        }
    }

    private void promoteToForeground(String text) {
        notificationText = text == null || text.isBlank()
                ? "Pebble Time is running"
                : text;
        Notification notification = buildNotification(notificationText);
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.UPSIDE_DOWN_CAKE) {
            startForeground(
                    NOTIFICATION_ID,
                    notification,
                    ServiceInfo.FOREGROUND_SERVICE_TYPE_SPECIAL_USE
            );
        } else {
            startForeground(NOTIFICATION_ID, notification);
        }
        foreground = true;
    }

    private void demoteFromForeground() {
        if (!foreground) {
            return;
        }
        stopForeground(STOP_FOREGROUND_REMOVE);
        foreground = false;
    }

    private void createNotificationChannel() {
        NotificationManager manager = getSystemService(NotificationManager.class);
        if (manager == null || manager.getNotificationChannel(NOTIFICATION_CHANNEL_ID) != null) {
            return;
        }
        NotificationChannel channel = new NotificationChannel(
                NOTIFICATION_CHANNEL_ID,
                "Pebblehertz runtime",
                NotificationManager.IMPORTANCE_LOW
        );
        channel.setDescription(
                "Keeps the selected Pebble watchface active on the rear display."
        );
        channel.setSound(null, null);
        channel.enableVibration(false);
        channel.enableLights(false);
        channel.setShowBadge(false);
        manager.createNotificationChannel(channel);
    }

    private Notification buildNotification(String text) {
        Intent openIntent = new Intent(
                this,
                com.manufacttest.pebblereardisplay.ui.MainActivity.class
        ).addFlags(Intent.FLAG_ACTIVITY_CLEAR_TOP | Intent.FLAG_ACTIVITY_SINGLE_TOP);
        PendingIntent open = PendingIntent.getActivity(
                this,
                0,
                openIntent,
                PendingIntent.FLAG_UPDATE_CURRENT | PendingIntent.FLAG_IMMUTABLE
        );
        Intent stopIntent = new Intent(this, PebbleRuntimeService.class)
                .setAction(ACTION_STOP);
        PendingIntent stop = PendingIntent.getService(
                this,
                1,
                stopIntent,
                PendingIntent.FLAG_UPDATE_CURRENT | PendingIntent.FLAG_IMMUTABLE
        );
        return new Notification.Builder(this, NOTIFICATION_CHANNEL_ID)
                .setSmallIcon(R.drawable.ic_notification)
                .setContentTitle("Pebblehertz is on air")
                .setContentText(text)
                .setContentIntent(open)
                .setCategory(Notification.CATEGORY_SERVICE)
                .setVisibility(Notification.VISIBILITY_PUBLIC)
                .setOngoing(true)
                .setOnlyAlertOnce(true)
                .setShowWhen(false)
                .addAction(R.drawable.ic_notification, "Stop", stop)
                .build();
    }
''',
              '''    private void scheduleTaskRemovalRecovery() {
        AppPreferences preferences = new AppPreferences(this);
        if (!RuntimeRecoveryPolicy.shouldRecover(
                preferences.getRuntimeMode(),
                preferences.hasSavedWatchfaceSelection()
        )) {
            return;
        }

        Intent recovery = new Intent(this, PebbleRuntimeService.class)
                .setAction(ACTION_RECOVER_TASK);
        PendingIntent pending = PendingIntent.getForegroundService(
                this,
                RECOVERY_REQUEST_CODE,
                recovery,
                PendingIntent.FLAG_UPDATE_CURRENT | PendingIntent.FLAG_IMMUTABLE
        );
        AlarmManager alarm = getSystemService(AlarmManager.class);
        if (alarm != null) {
            alarm.setAndAllowWhileIdle(
                    AlarmManager.ELAPSED_REALTIME_WAKEUP,
                    SystemClock.elapsedRealtime() + RECOVERY_DELAY_MILLIS,
                    pending
            );
        }
        try {
            startForegroundService(recovery);
        } catch (RuntimeException ignored) {
            // The AlarmManager PendingIntent remains as the fallback.
        }
    }

    private void cancelTaskRemovalRecovery() {
        PendingIntent pending = PendingIntent.getForegroundService(
                this,
                RECOVERY_REQUEST_CODE,
                new Intent(this, PebbleRuntimeService.class).setAction(ACTION_RECOVER_TASK),
                PendingIntent.FLAG_NO_CREATE | PendingIntent.FLAG_IMMUTABLE
        );
        if (pending == null) {
            return;
        }
        AlarmManager alarm = getSystemService(AlarmManager.class);
        if (alarm != null) {
            alarm.cancel(pending);
        }
        pending.cancel();
    }

    private void updateNotification(String ignoredText) {
        notificationText = "Rear watchface active";
        if (new AppPreferences(this).isReliableRuntime() && !foreground) {
            promoteToForeground(notificationText);
        }
    }

    private void applyRuntimeModeInternal(String text) {
        if (new AppPreferences(this).isReliableRuntime()) {
            promoteToForeground(text);
        } else {
            demoteFromForeground();
        }
    }

    private void promoteToForeground(String ignoredText) {
        notificationText = "Rear watchface active";
        Notification notification = buildNotification();
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.UPSIDE_DOWN_CAKE) {
            startForeground(
                    NOTIFICATION_ID,
                    notification,
                    ServiceInfo.FOREGROUND_SERVICE_TYPE_SPECIAL_USE
            );
        } else {
            startForeground(NOTIFICATION_ID, notification);
        }
        foreground = true;
    }

    private void demoteFromForeground() {
        if (!foreground) {
            return;
        }
        stopForeground(STOP_FOREGROUND_REMOVE);
        foreground = false;
    }

    private void createNotificationChannel() {
        NotificationManager manager = getSystemService(NotificationManager.class);
        if (manager == null) {
            return;
        }
        NotificationChannel channel = new NotificationChannel(
                NOTIFICATION_CHANNEL_ID,
                "Pebblehertz",
                NotificationManager.IMPORTANCE_LOW
        );
        channel.setDescription("Keeps the rear watchface active.");
        channel.setSound(null, null);
        channel.enableVibration(false);
        channel.enableLights(false);
        channel.setShowBadge(false);
        manager.createNotificationChannel(channel);
    }

    private Notification buildNotification() {
        Notification.Builder builder = new Notification.Builder(this, NOTIFICATION_CHANNEL_ID)
                .setSmallIcon(R.drawable.ic_notification)
                .setContentTitle("Pebblehertz")
                .setContentText("Rear watchface active")
                .setCategory(Notification.CATEGORY_SERVICE)
                .setVisibility(Notification.VISIBILITY_SECRET)
                .setLocalOnly(true)
                .setOngoing(true)
                .setOnlyAlertOnce(true)
                .setShowWhen(false);
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.S) {
            builder.setForegroundServiceBehavior(Notification.FOREGROUND_SERVICE_DEFERRED);
        }
        return builder.build();
    }
'''
          )

          activity_path = "app/src/main/java/com/manufacttest/pebblereardisplay/ui/MainActivity.java"
          activity = Path(activity_path)
          text = activity.read_text()
          text = text.replace("import android.Manifest;\n", "")
          text = text.replace("import android.content.pm.PackageManager;\n", "")
          text = text.replace("    private static final int REQUEST_NOTIFICATIONS = 1002;\n", "")
          text = text.replace(
              "    private String renderedActiveStorageId;\n",
              "    private String renderedActiveStorageId;\n    private boolean renderedSelectionQueuedForWake;\n"
          )
          old = '''        String activeId = PebbleRuntimeService.getActiveStorageId();
        if (!sameStorageId(renderedActiveStorageId, activeId)) {
            renderCatalog();
        }
'''
          new = '''        String activeId = PebbleRuntimeService.getActiveStorageId();
        boolean queuedForWake = PebbleRuntimeService.isSelectionQueuedForWake();
        if (!sameStorageId(renderedActiveStorageId, activeId)
                || renderedSelectionQueuedForWake != queuedForWake) {
            renderCatalog();
        }
'''
          if text.count(old) != 1:
              raise SystemExit("MainActivity runtime listener block not found")
          text = text.replace(old, new, 1)
          old = '''        renderedActiveStorageId = activeId;
        WatchfaceMetadata selected = null;
'''
          new = '''        renderedActiveStorageId = activeId;
        renderedSelectionQueuedForWake = PebbleRuntimeService.isSelectionQueuedForWake();
        WatchfaceMetadata selected = null;
'''
          if text.count(old) != 1:
              raise SystemExit("MainActivity render state block not found")
          text = text.replace(old, new, 1)
          old = '''        TextView action = pixelText(
                active ? "ON AIR" : selected ? "APPLYING..." : "TAP TO APPLY >",
                11,
                active
                        ? getColor(R.color.accent_coral)
                        : selected
                        ? getColor(R.color.accent_yellow)
                        : getColor(R.color.text_muted)
        );
'''
          new = '''        boolean queuedForWake = selected
                && !active
                && PebbleRuntimeService.isSelectionQueuedForWake();
        TextView action = pixelText(
                active
                        ? "ON AIR"
                        : queuedForWake
                        ? "QUEUED FOR WAKE"
                        : selected
                        ? "APPLYING..."
                        : "TAP TO APPLY >",
                11,
                active
                        ? getColor(R.color.accent_coral)
                        : selected
                        ? getColor(R.color.accent_yellow)
                        : getColor(R.color.text_muted)
        );
'''
          if text.count(old) != 1:
              raise SystemExit("MainActivity action label block not found")
          text = text.replace(old, new, 1)
          old = '''    private void maybeRequestBackgroundSetup() {
    getWindow().getDecorView().post(() -> {
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.TIRAMISU
                && checkSelfPermission(Manifest.permission.POST_NOTIFICATIONS)
                != PackageManager.PERMISSION_GRANTED) {
            requestPermissions(
                    new String[]{Manifest.permission.POST_NOTIFICATIONS},
                    REQUEST_NOTIFICATIONS
            );
            return;
        }
        maybeShowBatteryPrompt();
    });
}

@Override
public void onRequestPermissionsResult(
        int requestCode,
        String[] permissions,
        int[] grantResults
) {
    super.onRequestPermissionsResult(requestCode, permissions, grantResults);
    if (requestCode == REQUEST_NOTIFICATIONS) {
        maybeShowBatteryPrompt();
    }
}
'''
          new = '''    private void maybeRequestBackgroundSetup() {
        getWindow().getDecorView().post(this::maybeShowBatteryPrompt);
    }
'''
          if text.count(old) != 1:
              raise SystemExit("MainActivity notification permission block not found")
          text = text.replace(old, new, 1)
          activity.write_text(text)

          changelog = Path("CHANGELOG_0.8.10.md")
          content = changelog.read_text()
          marker = "- Android 14+ foreground-service type and permission declarations are included.\n"
          addition = marker + "- Runtime mode selection now uses a custom Pebblehertz-styled dialog with Reliable visually recommended.\n- The service notification has no buttons, alerts or notification-permission prompt.\n- Reliable mode schedules a guarded restart after Titan 2 removes the app task.\n- Watchface changes during Schedule Sleep are queued and applied once after wake or charging.\n- Schedule Sleep sends only one QEMU pause command per sleep interval.\n"
          if marker not in content:
              raise SystemExit("Changelog marker not found")
          changelog.write_text(content.replace(marker, addition, 1))

          Path(".github/workflows/stabilize-stage1.yml").unlink()
          PY

      - name: Commit stabilization patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Stabilize reliable runtime and schedule sleep"
          git push origin HEAD:agent/0.8.10-reliable-runtime
